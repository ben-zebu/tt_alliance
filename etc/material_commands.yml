---
TABULAR_FATIGUE_LAW:
  keys: {"en": ["tabular_fatigue_law", "tabular_fatigue_law"]}
  type: single

  LAW_ID:
    keys: {"en": ["law_id", "law_id"]}
    type: string

  DESCRIPTION:
    keys: {"en": ["description", "description"]}
    type: string

  RCCM:
    keys: {"en": ["rccm", "rccm"]}
    type: single
    DESCRIPTION:
      keys: {"en": ["description", "description"]}
      type: string
    EDITION:
      keys: {"en": ["edition", "edition"]}
      type: string_array

  ASME:
    keys: {"en": ["asme", "asme"]}
    type: single
    DESCRIPTION:
      keys: {"en": ["description", "description"]}
      type: string
    EDITION:
      keys: {"en": ["edition", "edition"]}
      type: string_array

  EREF:
    keys: {"en": ["eref", "eref"]}
    type: real
  N_RATIO:
    keys: {"en": ["n_ratio", "n_ratio"]}
    type: real

  NA:
    keys: {"en": ["na", "na"]}
    type: real_array
  SA:
    keys: {"en": ["sa", "sa"]}
    type: real_array

POWER_FATIGUE_LAW:
  keys: {"en": ["power_fatigue_law", "power_fatigue_law"]}
  type: single

  LAW_ID:
    keys: {"en": ["law_id", "law_id"]}
    type: string

  DESCRIPTION:
    keys: {"en": ["description", "description"]}
    type: string

  RCCM:
    keys: {"en": ["rccm", "rccm"]}
    type: single
    DESCRIPTION:
      keys: {"en": ["description", "description"]}
      type: string
    EDITION:
      keys: {"en": ["edition", "edition"]}
      type: string_array

  ASME:
    keys: {"en": ["asme", "asme"]}
    type: single
    DESCRIPTION:
      keys: {"en": ["description", "description"]}
      type: string
    EDITION:
      keys: {"en": ["edition", "edition"]}
      type: string_array

  EREF:
    keys: {"en": ["eref", "eref"]}
    type: real
  N_RATIO:
    keys: {"en": ["n_ratio", "n_ratio"]}
    type: real
  SEQ:
    keys: {"en": ["seq", "seq"]}
    type: real

  ALPHA:
    keys: {"en": ["alpha", "alpha"]}
    type: real
  BETA:
    keys: {"en": ["beta", "beta"]}
    type: real

POLYNOMIAL_FATIGUE_LAW:
  keys: {"en": ["polynomial_fatigue_law", "polynomial_fatigue_law"]}
  type: single

  LAW_ID:
    keys: {"en": ["law_id", "law_id"]}
    type: string

  DESCRIPTION:
    keys: {"en": ["description", "description"]}
    type: string

  RCCM:
    keys: {"en": ["rccm", "rccm"]}
    type: single
    DESCRIPTION:
      keys: {"en": ["description", "description"]}
      type: string
    EDITION:
      keys: {"en": ["edition", "edition"]}
      type: string_array

  ASME:
    keys: {"en": ["asme", "asme"]}
    type: single
    DESCRIPTION:
      keys: {"en": ["description", "description"]}
      type: string
    EDITION:
      keys: {"en": ["edition", "edition"]}
      type: string_array

  EREF:
    keys: {"en": ["eref", "eref"]}
    type: real
  N_RATIO:
    keys: {"en": ["n_ratio", "n_ratio"]}
    type: real
  SEQ:
    keys: {"en": ["seq", "seq"]}
    type: real

  COEFFICIENTS:
    keys: {"en": ["coefficients", "coefficients"]}
    type: real_array

BASE_MATERIAL:
  keys: {"en": {"base_material", "base_material"}}
  type: single

  MATERIAL_ID:
    keys: {"en": ["material_id", "material_id"]}
    type: string

  FATIGUE_LAW_ID:
    keys: {"en": ["fatigue_law_id", "fatigue_law_id"]}
    type: string

  STEEL_TYPE:
    keys: {"en": ["steel_type", "steel_type"]}
    type: string
  
  CORRECTION:
    keys: {"en": ["correction", "correction"]}
    type: string

  KF:
    keys: {"en": ["kf", "kf"]}
    type: real

  M_FACTOR:
    keys: {"en": ["m_factor", "m_factor"]}
    type: real

  N_FACTOR:
    keys: {"en": ["n_factor", "n_factor"]}
    type: real


RETURN:
  keys: {"fr": ["reto", "retour"], "en": ["retu", "return"]}
  type: single
